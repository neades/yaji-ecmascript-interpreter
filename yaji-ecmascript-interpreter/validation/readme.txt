Fesi validation suite - jm lugrin - 1998

This directory contains a set of validation tests for the
FESI interpreter. The individual files can be loaded using
the @test command, or the whole set can be executed using
the validate batch file in the bin directory.

